{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter une catégorie {% endblock %}

{% block body %}
    <a href="{{ path('app_admin_category') }}">Annuler</a>
    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <button type="submit" class="btn btn-primary">Créer</button>
    {{ form_end(form) }}
{% endblock %}
