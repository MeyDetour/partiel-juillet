{% extends 'admin/base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
    <a href="{{ path('admin_add_voice') }}">Ajouter une version</a>

    <div class="d-flex flex-column">
        {% for cat in categories %}
            <div class="d-flex fl border-dark">
                <a href="{{ path('admin_edit_voice',{'id':cat.id}) }}">Modifier</a>
                <a href="{{ path('app_admin_voice_remove',{'id':cat.id}) }}">Supprimer</a>
                {{ cat.name }}

            </div>

        {% endfor %}
    </div>

{% endblock %}
